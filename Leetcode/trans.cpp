#include "solution.h"

void trans()
{
    string s;
    cin>>s;
    int length = s.size();
    if(length < 10)
        n = (length - 9)/2 + 9;
    else
        n = length;
    int pos[n];
    int k = 0;
    map<int, int> m;

}
